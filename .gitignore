.env
.monkey-army/
*.log
__pycache__/
.DS_Store
.claude/handoffs/

# Screenshots and recordings — never commit these (see repo rules).
Screenshot*
*.png
# ...except deliberate repo artwork, which lives only here.
!docs/assets/*.png
*.gif
*.mov
*.mp4

# Session handoffs are local memory, never project source.
.claude/handoffs/

# Private planning notes, kept offline.
docs/MONKEY_ARMY_IMPLEMENTATION_PLAN.md
