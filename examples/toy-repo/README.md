# Toy Repository

Toy repository used by monkey-army's validation plan (docs/VALIDATION.md). Copy it to /tmp, git init, and point the monkeys at it.

```bash
cp -r examples/toy-repo /tmp/toy && cd /tmp/toy && git init -b main && git add . && git commit -m init
```
