# <title — imperative, ≤ 80 chars>

## Change
In `<path/to/file.py>`, <exactly what to add/modify/remove>. Place it <where: after function X /
at the end of class Y>.

## Signatures / types to match
```<lang>
<paste exact signatures, dataclasses, interfaces>
```

## Conventions
- <naming, error handling style, import style, formatter, docstring rule> (name the file that
  shows the pattern to copy, e.g. "mirror `foo()` in the same module")

## Read first
- `<path>` (why)
- `<path>` (why)

## Allowed files
- `<path or glob>` (only these)

## Definition of done
- <observable outcome 1>
- <observable outcome 2>

## Acceptance command
`<exact command that exits 0 when done>`

## Do not
- Do not touch other files. Do not redesign. Do not reformat unrelated code. Implement exactly
  this.
